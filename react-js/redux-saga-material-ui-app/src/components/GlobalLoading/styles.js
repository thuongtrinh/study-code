import { styled } from "@mui/material";

export const GlobalLoadingDiv = styled("div")({
  position: "fixed",
  left: 0,
  right: 0,
  top: 0,
  bottom: 0,
  zIndex: 99,
  background: "rgba(0, 0, 0, 0.4)",
});

export const IconImg = styled("img")({
  position: "fixed",
  left: 0,
  right: 0,
  marginLeft: "auto",
  marginRight: "auto",
  top: "40%",
  width: 100,
});
